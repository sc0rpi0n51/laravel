@extends('layouts.admin') @section('title','Nuevo estudiante') @section('content')<h2>Nuevo estudiante</h2>@if($errors->any())<ul>@foreach($errors->all() as $error)<li>{{$error}}</li>@endforeach</ul>@endif<form method="POST" action="{{route('admin.estudiantes.store')}}">@csrf<label>Nombre <input name="name" value="{{old('name')}}" required></label><br><label>Correo <input type="email" name="email" value="{{old('email')}}" required></label><br><label>Contraseña <input type="password" name="password" required></label><br><label>Confirmar <input type="password" name="password_confirmation" required></label><br><label>Matrícula <input name="matricula" value="{{old('matricula')}}" required></label><br><label>Grado <select name="grado_id" required>@foreach($grados as $g)<option value="{{$g->id}}">{{$g->nombre}}</option>@endforeach</select></label><br><label>Sección <select name="seccion_id" required>@foreach($secciones as $s)<option value="{{$s->id}}">{{$s->nombre}}</option>@endforeach</select></label><br><button>Guardar</button></form>@endsection
